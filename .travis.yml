language: java
jdk: oraclejdk8

addons:
    postgresql: "9.4"

before_script:
    - psql -c "create database unhypemboot;" -U postgres

before_install:
    - chmod +x gradlew

script:
    - ./gradlew test -Dspring.profiles.active=travis
