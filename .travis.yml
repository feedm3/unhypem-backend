language: java
jdk: oraclejdk8

env:
   - spring.datasource.password=

addons:
    postgresql: "9.4"

cache:
  directories:
    - $HOME/.gradle

before_script:
    - psql -c 'create database unhypem-boot;' -U postgres

before_install:
    - chmod +x gradlew
