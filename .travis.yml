language: java
jdk: oraclejdk8

env:
    - SPRING_PROFILES_ACTIVE=travis

addons:
    postgresql: "9.4"

before_script:
    - psql -c "create database unhypemboot;" -U postgres

before_install:
    - chmod +x gradlew
